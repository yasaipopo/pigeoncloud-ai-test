# 網羅フロー設計: payment.spec.js (R-161〜R-172, 12要件)

**作成**: Claude + gemcli A (プロダクトコード) + gemcli B (既存テスト) 統合版
**ステータス**: 🟢 Step [3] ユーザー承認① 待ち

---

## 対象要件 12 件 (全て priority:high)

| ID | 要件 | gemcli A 実装確認 |
|----|------|------------------|
| R-161 | PayPal決済 | ✅ `PaypalService.php` (Sandbox要) |
| R-162 | Stripe決済 | ✅ `StripeService.php` (Sandbox要) |
| R-163 | 定期支払い | ✅ `invoice.paid` Webhook依存 |
| R-164 | 支払い履歴管理 | ✅ `admin_invoices` テーブル |
| R-165 | 請求書生成 | ✅ PDF生成 (LibreOffice依存) |
| R-166 | 返金処理 | ❌ **未実装** (Stripe Refunds 呼び出しなし) |
| R-167 | 契約情報管理 | ✅ `payment_info` テーブル |
| R-168 | 支払い方法変更 | ✅ `stripeCardUpdate` |
| R-169 | プラン変更 (日割り) | ✅ Stripe `proration_behavior` |
| R-170 | 解約処理 | △ バックエンド実装あるが **UI ボタン無し** |
| R-171 | 決済エラー | △ Webhook `invoice.payment_failed` 依存 |
| R-172 | 設定エラー | ✅ `checkPrice` で不一致検出 |

---

## 既存 payment.spec.js (9 テスト、タグなし、大半 skip)

| 行 | ケース | 推定要件 | 状態 |
|----|-------|---------|-----|
| 84 | pay-010 支払いページ表示 | R-162 | Active |
| 106 | pay-020 プラン変更フォーム | R-169 | Skipped (Stripe key 不足) |
| 143 | pay-030 Stripe checkout iframe | R-162 | Skipped |
| 164 | pay-040 テストカード入力 | R-162 | Skipped |
| 217 | pay-050 ユーザー数 バリデーション | R-171/172 | Skipped |
| 254 | pay-060 支払い履歴 API | R-164 | Active |
| 300 | pay-070 カードブランド表示 | R-168 | Active |
| 328 | pay-080 請求情報メニュー + 領収書DL | R-165 | Active (条件付き) |
| 374 | pay-090 ユーザー数変更反映 | R-169 | Active |

---

## 🎯 統合設計

### 方針
1. **既存 active 4 テストを強化 + タグ付与** (pay-010, 060, 070, 080, 090)
2. **Stripe Sandbox 実決済は環境依存で skip** → UI 存在確認/バリデーションで代替
3. **PayPal も Sandbox 依存で UI 確認のみ**
4. **R-166 返金は未実装 → skip**
5. **R-170 解約 UI 無し → バックエンド動作検証のみ**
6. **Webhook 依存 (R-163 / R-171) は E2E では検証困難 → skip**

### ケース表

#### ✅ 既存テスト強化 + タグ付与 (5件)
| 行 | ケース | タグ付与 | 備考 |
|----|-------|---------|------|
| 84 | pay-010 | R-162 | タグ付与のみ |
| 254 | pay-060 | R-164 | タグ付与 + 履歴画面の UI 要素確認を追加 |
| 300 | pay-070 | R-168 | タグ付与 + カード更新フォームの存在確認追加 |
| 328 | pay-080 | R-165 | タグ付与 + 領収書DL 実動作確認 |
| 374 | pay-090 | R-169 | タグ付与 + ユーザー数変更の表示確認追加 |

#### 🆕 新規追加 (6件)
| ケース | 要件 | 内容 |
|-------|------|-----|
| pay-100 契約情報画面 | R-167 | /admin/master-settings で プラン・ユーザー数表示確認 |
| pay-110 金額バリデーション | R-172 | フロントから不正金額を `checkPrice` 経由で送信 → エラー |
| pay-120 必須項目バリデーション | R-172 | 必須項目空で送信 → エラー |
| pay-130 PayPal ボタン表示 | R-161 | 決済画面で PayPal ボタンの存在確認 (Sandbox 実決済は skip) |
| pay-140 Stripe iframe 表示 | R-162 | Stripe checkout iframe の DOM 存在確認 (実決済は skip) |
| pay-150 期限切れ通知表示 | R-171 | 期限切れ状態のテストクライアント用意 → 通知表示確認 |

#### ❌ スキップ (test-env-limitations.md 追加) - 4件

| ケース | 要件 | 理由 |
|-------|------|-----|
| Stripe 実決済完了 | R-162 | Sandbox key/テストカード環境依存 |
| PayPal 実決済完了 | R-161 | PayPal Sandbox アカウント依存 |
| Webhook 定期支払い | R-163 | Webhook シミュレーション要 (Stripe CLI) |
| Webhook 決済失敗 | R-171 | 同上 |
| **R-166 返金処理** | R-166 | **プロダクト未実装** |
| R-170 解約処理 | R-170 | UI ボタン無し、バックエンド経由のみ |

---

## ✅ Step [3] ユーザー承認結果 (2026-04-20)

### 承認済み判断

1. ✅ **R-166 返金**: **スキップ**（プロダクト未実装）→ 機能追加 backlog 化。`test-env-limitations.md` 記録済
2. ✅ **R-170 解約**: **スキップ**（UI 無し）→ UI 追加 PR マージ後に再検討。記録済
3. ✅ **Stripe Sandbox**: staging は **すでに Sandbox 構成**とのユーザー確認（2026-04-20）→ R-162 実決済テストは **実装対象**（スキップから除外）
4. ✅ **PayPal**: **E2E 対象外**（ユーザー指示「テスト不要」）→ pay-130 で DOM 存在確認のみ、実走スキップ
5. ✅ **Webhook 系 (R-163/R-171)**: Stripe CLI 統合 or Integration Test で対応 → E2E スキップ継続
6. ⏳ **スキップ率 15.8%**: 原因分析完了 → `.claude/skip-rate-analysis-20260420.md` 参照（短期 7.9%、中期 4.5% 到達計画）

### 修正後のケース一覧

#### ✅ 既存強化 + タグ付与 (5件) — 変更なし
pay-010 / pay-060 / pay-070 / pay-080 / pay-090

#### 🆕 新規追加 (6件) — 一部修正
| ケース | 要件 | 内容 | 修正点 |
|---|---|---|---|
| pay-100 契約情報画面 | R-167 | 維持 | — |
| pay-110 金額バリデーション | R-172 | 維持 | — |
| pay-120 必須項目バリデーション | R-172 | 維持 | — |
| pay-130 PayPal ボタン表示のみ | R-161 | **DOM 存在確認のみに限定**、実決済スキップ | ユーザー判断反映 |
| pay-140 Stripe Sandbox 実決済 | R-162 | **Sandbox でテストカード決済完了**まで実施 | ← Sandbox OK なので拡張 |
| pay-150 期限切れ通知表示 | R-171 | 維持 | — |

### スキップ率（更新後）
- payment 起因スキップ: R-161 PayPal / R-163 Webhook / R-166 返金 / R-170 解約 / R-171 Webhook失敗 = **5 件**
- R-162 Stripe 実決済は Sandbox で実装するため**スキップから除外**
- 全体: 12 件 (15.8%) → 後続のモック/CLI 導入で段階的に削減

# 引き継ぎ資料 2026-04-19 02:49 JST

## 🔴 次セッション冒頭で必ず読むファイル（順番通り）

1. `/Users/yasaipopo/PycharmProjects/pigeon-test/CLAUDE.md` — **「黄金ループ」セクションを必ず読む**
2. `.claude/e2e-coverage-roadmap.md` — 短・中・長期の全体施策
3. `.claude/knowledge-spec-quality-checklist.md`
4. `.claude/knowledge-detailed-flow-writing-rule.md`
5. `.claude/knowledge-e2e-failure-triage.md`
6. `.claude/product-bugs.md` — 既知プロダクトバグ一覧
7. `.claude/test-implementation-notes.md` — B002 他の実装メモ

---

## 🚨 絶対に守ること（このセッションで頻繁に破ろうとした失敗例あり）

### 1. 黄金ループ 8 ステップを順番通り実施（CLAUDE.md 参照）

```
[1] 調査 (gemcli 並列)
[2] 実機確認 (Claude 手動 / Playwright codegen)
[3] 実装 (gemcli、@requirement タグ必須)
[4] 品質ゲート (python3 scripts/quality-gate.py)
[5] テスト実行 (直列、workers=1)
[6] sheet.html アップロード (+ sync-results 必須)
[7] レビュー URL 生成 → ユーザーに提示:
    https://dezmzppc07xat.cloudfront.net/sheet.html?spec=XXX&caseNo=YYY&review=pending&reviewLabel=説明
[8] ユーザー OK → コミット / NG → [1]-[5] のどれかに戻る
```

**必ず [7] でユーザーに確認を求めてから次に進む**。勝手に進めない。

### 2. 絶対禁止事項

- ❌ `test.skip()` 新規追加（既存 `test.skip(fileBeforeAllFailed, ...)` はOK）
- ❌ `expect(x || true).toBeTruthy()` 等の常 true assertion
- ❌ `expect(...).not.toContain('不明なエラー')` の削除・コメントアウト
- ❌ navbar / Internal Server Error だけ確認して pass とする浅いテスト
- ❌ waitForTimeout ハードコード (10秒超)
- ❌ `@requirement(R-XXX)` タグなしの新規 test 追加

### 3. プロダクトバグと Spec バグの切り分け

- テストが fail したら `.claude/knowledge-e2e-failure-triage.md` の基準で分類
- プロダクトバグ疑いなら:
  - テストは **fail のまま残す**（assertion を緩めない）
  - `.claude/product-bugs.md` に詳細記録
- Spec バグ（セレクター不一致等）なら:
  - spec.js を修正
  - 再実行で pass 確認

### 4. gemcli と Claude の役割分担

- **gemcli (並列 5 本程度まで)**: 調査・ボイラープレート生成・静的解析・fail 分類
- **Claude (直列)**: 計画・実機確認・統合・レビュー・コミット・ユーザー対話

### 5. pigeon_cloud 修正は必ず tmprepo 経由

ローカル `/Users/yasaipopo/PhpStormProjects/PopoframeworkSlim` を直接編集禁止。

---

## 現在のブランチと PR 状態

- ブランチ: `feat/createenv-timeout-and-nt05`
- 本日のコミット (4件 push 済み):
  ```
  42fa290 refactor: 1 spec 1 環境化 + bug-* spec を親に統合
  9636f21 fix: 黄金ループ実行インフラ改善
  2ebefaa feat: 黄金ループ 1件目 B002 IP制限 実装
  01cd2c0 docs: CLAUDE.md に黄金ループ手順を追記
  ```
- **PR 未作成**（必要なら ki 作成）

---

## 完了済み（本日）

### 分析フェーズ
- Slack #テスト-staging 過去4週間のバグ16件を構造化 → `/tmp/bug-catalog.json`
- カバレッジ突合 → `/tmp/coverage-all.json` （1件のみ covered = 6%）
- ギャップ分析 → `/tmp/gap-summary.md` `/tmp/gap-matrix.json` （254要件、真カバー率 39%）
- パターン分類 → `/tmp/gap-patterns.md` （Shallow UI Check / State / Missing Specs など）

### 基盤整備
- `scripts/quality-gate.py` — 静的解析（test.skip, Shallow, assertion欠如検知）
- `scripts/coverage-tracker.py` — @requirement ID × test 対応表生成
- `.claude/coverage-tracker.json` — 現在のカバー状況
- `e2e-viewer/frontend/sheet.html` — URL パラメータ + レビューバナー + OK/NG ボタン（**デプロイ済み**）
- `e2e-viewer/backend/lambda/index.py` — POST `/pipeline/review` endpoint（**デプロイ済み**）

### 実装フェーズ
- `tests/users-permissions.spec.js` に B002 IP制限テスト 5件追加 (up-ip-040〜044, @requirement R-IP-001〜005)
- `.claude/product-bugs.md` に B002 の根本原因記録（`NetCommon::getIp()` プライベートIP除外）
- `.claude/test-implementation-notes.md` に実装調査メモ

### リファクタリング
- auth.spec.js: 6 環境 → 1 環境
- bug-b001.spec.js → csv-export.spec.js に統合（削除）
- bug-b005.spec.js → data-operations.spec.js に統合（削除）
- bug-b007.spec.js + bug-b012.spec.js → **kintone.spec.js** に統合

### インフラ改善
- `playwright.config.js`: JSON 出力先を `reports/agent-N/playwright-results.json` に明示
- `tests/helpers/create-test-env.js`: ログインリトライ 3→6 回、timeout 15s→30s
- `tests/users-permissions.spec.js` UP-B002: test.setTimeout(180000)、セレクター待機短縮

---

## ⚠️ 未解決の課題（次セッション着手）

### 優先度 🔴 最高

#### 1. staging 一時 DB の蓄積問題
- 本日の実行で大量の create-trial → 200+ 新規 DB 作成
- staging RDS ストレージ圧迫リスク
- **対応**:
  - (a) pigeon_cloud に HTTP delete-client API 追加（tmprepo 経由で PR）
  - (b) createTestEnv に `cleanup: true` オプション追加 → afterAll で自動削除
  - (c) 古い `tmp-testai-*` DB の一括削除（staging で `delete-client` CLI）

#### 2. upload_results.py の nested suites 対応
- 現状: Playwright JSON の nested `suites[].suites[].specs` 構造を再帰処理できず 0件として扱う
- 修正: `_collect_specs` 関数を再帰強化
- ファイル: `e2e-viewer/upload_results.py` L176

#### 3. B002 テスト実行の完遂
- 現状: 3 分 timeout で fail 中（セレクター or UI 操作の問題）
- `tests/users-permissions.spec.js` L5040〜 の `up-ip-04X` の実 UI 確認が必要
- Playwright codegen で実操作 → セレクター更新

### 優先度 🟡 中

#### 4. 黄金ループ 2件目以降（残 148件の高優先度要件）

`.claude/coverage-tracker.json` の priority: high から選ぶ。

順序（gap-summary.md TOP20 より）:
- **認証・認可系**（2FA/SAML/SSO/セッション）— auth.spec.js に追加
- **IP制限系** — users-permissions.spec.js (B002 は完遂してから次)
- **クライアント証明書** — user-security.spec.js
- **決済エラー系** — payment.spec.js
- **ファイル権限** — field-image-file.spec.js

**1件ずつ黄金ループで**。適当に並列で 10件やるとか絶対にしない。

#### 5. CI に quality-gate.py 組込
- GitHub Actions で PR 時に自動実行
- violations > 0 で block

---

## 🎯 次セッション推奨アクションプラン

```
Phase 1 (30分): 環境整備
  1-1. 古い tmp-testai DB cleanup（手動 or script）
  1-2. upload_results.py の nested suites 対応修正
  1-3. pigeon_cloud に delete-client HTTP API 追加 PR 作成

Phase 2 (1時間): B002 完遂
  2-1. Playwright codegen で実機 UI 確認
  2-2. up-ip-040〜044 のセレクター調整
  2-3. 単体実行 → アップロード → レビュー URL 提示
  2-4. ユーザー承認待ち

Phase 3 (2-3時間): 黄金ループ 2件目〜
  次候補: 認証・認可系 2FA (R-AUTH-002)
  黄金ループ 8 ステップを厳守
```

---

## 参考情報

### 環境
- AWS profile: lof-dev
- staging API: https://ausatkfji9.execute-api.ap-northeast-1.amazonaws.com
- sheet.html: https://dezmzppc07xat.cloudfront.net/sheet.html
- test base: https://ai-test.pigeon-demo.com

### URL パラメータ例
```
https://dezmzppc07xat.cloudfront.net/sheet.html?spec=users-permissions&caseNo=up-ip-040,up-ip-041&review=pending&reviewLabel=B002 IP制限テスト確認
```

### 頻出コマンド
```bash
# 単体実行
source .env && AGENT_NUM=1 SKIP_GLOBAL_SETUP=1 npx playwright test tests/XXX.spec.js -g "YY" --retries=0 --workers=1 --reporter=list,json

# 品質ゲート
python3 scripts/quality-gate.py tests/XXX.spec.js

# カバレッジ
python3 scripts/coverage-tracker.py --check

# アップロード
E2E_API_URL=https://ausatkfji9.execute-api.ap-northeast-1.amazonaws.com python3 e2e-viewer/upload_results.py --reports-dir reports/agent-1 --agent-num 1

# sync-results (必須)
source .env && TOKEN=$(python3 -c "import hashlib; print(hashlib.sha256('pigeon-e2e-2026pigeon-e2e-viewer-salt-2026'.encode()).hexdigest())") && curl -s -X POST -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" "${E2E_API_URL}/pipeline/sync-results" -d "{\"runId\":\"${RUN_ID}\",\"env\":\"staging\"}"
```

### /tmp に残る重要ファイル（次セッションでも参照可）
- `/tmp/gap-matrix.json` 325KB — 254要件の詳細
- `/tmp/gap-summary.md` — TOP20 ギャップ
- `/tmp/bug-catalog.json` — Slack 報告バグ 16件構造化
- `/tmp/coverage-all.json` — 各バグ×E2E 対応表
- `/tmp/spec-matrix-docs.json` + `-code.json` + `-e2e.json`

---

## 💬 ユーザー指示の要点（本日受けた重要な指摘）

1. **「バグ報告を追加するだけでなく、網羅的にカバーできるコードに全体を変える」**
   - 個別 bug-b00X.spec.js を作るのではなく、親 spec に観点を統合すべき
   - → kintone/csv-export/data-operations に統合済み

2. **「機能網羅 → レイアウト」の2段階**
   - まず機能検証を網羅的に
   - その後レイアウト（ビジュアルリグレッション等）

3. **「テスト作成後・実行後の動画をユーザーが確認できる仕組み」**
   - sheet.html URL パラメータ + レビューバナー実装済み
   - 各ループで必ず [7] でユーザーに URL 提示

4. **「適当なテストやスキップは絶対にしない」「ステップバイステップで丁寧に」**
   - assertion 緩和禁止（再三の警告）
   - gemcli で並列処理しつつ、Claude が統合レビュー

5. **「1 spec 1 環境にできる限り統一」**
   - auth.spec.js は今日 6→1 に統合
   - 他も随時統合推奨

6. **「push 判断は都度確認」**
   - ただし本日は「B でいいや」で直接 push 許可済み
   - 以後は都度確認

---

**本日の作業時間**: 約 10 時間
**コミット数**: 4
**追加テスト**: 5 test (B002)
**削除した冗長ファイル**: 4 (bug-b00X.spec.js + yaml)
**新規ツール**: 2 (quality-gate.py / coverage-tracker.py)
**sheet.html 機能追加**: URL フィルタ + レビュー機構

次セッションも**黄金ループ厳守**で進めてください。
